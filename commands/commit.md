---
description: Stage all changes and generate a commit message
allowed-tools: bash
---
Run `git status` and `git diff` to see what's changed. Then run `git add -A` and create a commit with a concise, conventional-commit-style message summarizing the changes.
